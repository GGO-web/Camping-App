import { StyleSheet } from 'react-native';

export const loginFormStyles = StyleSheet.create({
   formGroup: {
      marginBottom: 20,
   },
   inputHelperText: {
      marginLeft: 'auto',
      fontSize: 12,
   },
});
